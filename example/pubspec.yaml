name: taskflow_example
description: Example app demonstrating LocaleSweep — a localization QA tool for Flutter.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  locale_sweep:
    path: ../
